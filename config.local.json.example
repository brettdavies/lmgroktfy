{
  /* 
   * IMPORTANT NOTES:
   * 1. HTTPS is required for cross-origin requests to work properly
   * 2. The API server must support CORS for your local development domain
   * 3. If you encounter CORS errors, ensure your API endpoint supports requests from http://localhost:8080
   */
  "apiBaseUrl": "https://development-api-domain.com",
  "port": 3000,
  "mockResponses": false,
  "logRequests": true,
  "debugMode": true
} 
